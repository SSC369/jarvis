#import "../../../fragments/TaskFields.graphql"

query GetRecordDetail($id: ID!) {
  record(id: $id) {
    __typename
    ... on Task {
      ...TaskFields
    }
    ... on RecordNotFound {
      message
    }
  }
}
