#import "../../../fragments/CaptureTurnFields.graphql"

query GetCaptureHistory($cursor: String) {
  captureHistory(cursor: $cursor) {
    nextCursor
    items {
      ...CaptureTurnFields
    }
  }
}
